

// Scroll Reveal 

const sr = ScrollReveal({
    origin:'top',
    distance:'60px',
    duration:2500,
    delay:400,
    // reset:true,
})

sr.reveal(`.right-des-text,nav`)
sr.reveal(``,{delay:1000,origin:'bottom'})
sr.reveal(`.right-des-icon`,{origin:'left'})
sr.reveal(`.mobile-nav`,{origin:'right'})










